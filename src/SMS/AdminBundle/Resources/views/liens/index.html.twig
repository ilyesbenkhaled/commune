{% extends 'SMSAdminBundle::base.html.twig' %}

{% block body %}
<script src="jquery.js"></script>

<script>


	$('#generer').click(function() {
	  $.ajax({
		type: 'POST',
		url: 'functions.php',
		timeout: 3000,
		data:
		{
			myFunction:'generer'
		},                
		success: function(data) {
		  alert(data); 
		},
		error: function() {
		  alert('La requête n\'a pas abouti'); 
		}
	  });   
	});
</script>
<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Liste de texte</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            Liste de texte
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">

        <thead>
            <tr>
                <th>Id</th>
                <th>Image</th>
                <th>Url</th>
                <th>Datecreation</th>
                <th>Dateupdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lien in liens %}
            <tr>
                <td><a href="{{ path('liens_show', { 'id': lien.id }) }}">{{ lien.id }}</a></td>
                <td>{{ lien.image }}</td>
                <td>{{ lien.url }}</td>
                <td>{% if lien.dateCreation %}{{ lien.dateCreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if lien.dateUpdate %}{{ lien.dateUpdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('liens_show', { 'id': lien.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('liens_edit', { 'id': lien.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('liens_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}

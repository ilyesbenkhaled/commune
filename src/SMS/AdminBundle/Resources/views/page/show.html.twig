
{% extends 'SMSAdminBundle::base.html.twig' %}

{% block body %}
<script src="jquery.js"></script>

<script>


	$('#generer').click(function() {
	  $.ajax({
		type: 'POST',
		url: 'functions.php',
		timeout: 3000,
		data:
		{
			myFunction:'generer'
		},                
		success: function(data) {
		  alert(data); 
		},
		error: function() {
		  alert('La requête n\'a pas abouti'); 
		}
	  });   
	});
</script>
<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Liste de texte</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            Liste de texte
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
         
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ page.content }}</td>
            </tr>
            <tr>
                <th>Titremenu</th>
                <td>{{ page.titreMenu }}</td>
            </tr>
            <tr>
                <th>Urlfile</th>
                <td>{{ page.urlFile }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('page_index') }}" class="form-control">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('page_edit', { 'id': page.id }) }}" class="form-control">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="form-control">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
